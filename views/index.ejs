<!DOCTYPE html>
<html>
<head>
  <title>Currency Converter</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="card shadow p-4">
      <h2 class="text-center mb-4">Currency Converter</h2>
      <form action="/convert" method="POST" class="row g-3">
        <div class="col-md-4">
          <input type="number" name="amount" class="form-control" placeholder="Amount" required>
        </div>
        <div class="col-md-3">
          <input type="text" name="from" class="form-control" placeholder="From (e.g., USD)" required>
        </div>
        <div class="col-md-3">
          <input type="text" name="to" class="form-control" placeholder="To (e.g., INR)" required>
        </div>
        <div class="col-md-2 d-grid">
          <button class="btn btn-primary">Convert</button>
        </div>
      </form>
      <% if (result) { %>
        <div class="alert alert-success mt-4 text-center"><%= result %></div>
      <% } %>
      <% if (error) { %>
        <div class="alert alert-danger mt-4 text-center"><%= error %></div>
      <% } %>
    </div>
  </div>
</body>
</html>

